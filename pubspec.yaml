name: tasklist
description: "A new Flutter project."
publish_to: 'none'

version: 0.0.1+1

environment:
  sdk: '>=2.19.4 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  english_words: ^4.0.0
  provider: ^6.0.0

  # USE ESTAS VERSÕES EXATAS (Série Estável 2.x e 4.x)
  firebase_core: ^2.32.0
  firebase_auth: ^4.20.0
  cloud_firestore: 4.17.0
  file_picker: ^8.0.7
  ffmpeg_kit_flutter_audio: ^6.0.3
  path_provider: ^2.1.5
  audioplayers: ^6.5.1
  cloudinary_public: ^0.23.1
  flutter_card_swiper: ^7.2.0
  
dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true